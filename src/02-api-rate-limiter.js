// As the name suggests, we want to build a limiter that allows a user to make only 3 requests per minute.

function createRateLimiter(limit) {
    // Your code here
    
}
module.exports = { createRateLimiter }